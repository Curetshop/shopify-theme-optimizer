📝 APLICANDO CONTENIDO COMPLETO - PARTE 1/3: HEADER
{%- comment -%}
ENHANCED REVIEWS SECTION - PROFESSIONAL & MOBILE-FIRST
Updated by Pitágora V2.0 with professional header design
{%- endcomment -%}

<!-- ENHANCED PROFESSIONAL HEADER - MOBILE-FIRST -->
<div class="reviews-section-enhanced">
  <div class="reviews-header-professional">
    <div class="reviews-header-content">
      
      <!-- Title Section -->
      <div class="reviews-title-group">
        <h2 class="reviews-title">{{ section.settings.title | default: 'Reseñas de Clientes' }}</h2>
        <p class="reviews-subtitle">{{ section.settings.subtitle | default: 'Opiniones reales de nuestros compradores' }}</p>
      </div>
      
      <!-- Rating Display - Mobile Optimized -->
      <div class="reviews-rating-display">
        <div class="rating-score-container">
          <div class="rating-score">{{ section.settings.rating | default: 4.88 }}</div>
          <div class="rating-info">
            <div class="stars-container">
              {%- assign rating = section.settings.rating | default: 4.88 -%}
              {%- assign full_stars = rating | floor -%}
              {%- assign has_half_star = rating | modulo: 1 | round: 1 -%}
              
              {%- for i in (1..5) -%}
                {%- if i <= full_stars -%}
                  <span class="star star-full">★</span>
                {%- elsif i == full_stars | plus: 1 and has_half_star >= 0.5 -%}
                  <span class="star star-half">★</span>
                {%- else -%}
                  <span class="star star-empty">☆</span>
                {%- endif -%}
              {%- endfor -%}
            </div>
            <p class="rating-description">
              Basado en {{ section.settings.review_count | default: 127 }} reseñas verificadas
            </p>
          </div>
        </div>
        
        <!-- Trust Badge -->
        <div class="trust-badge">
          <svg class="check-icon" width="16" height="16" viewBox="0 0 24 24">
            <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41L9 16.17z"/>
          </svg>
          <span>Verificadas</span>
        </div>
      </div>
    </div>
    
    <!-- Separator -->
    <div class="reviews-separator"></div>
  </div>

  <!-- Reviews Content -->
  <div class="reviews-content">
    {%- if section.blocks.size > 0 -%}
      {%- for block in section.blocks -%}
        <div class="review-item" {{ block.shopify_attributes }}>
          <div class="review-header">
            <div class="review-rating">
              {%- assign review_rating = block.settings.rating | default: 5 -%}
              {%- for i in (1..5) -%}
                {%- if i <= review_rating -%}
                  <span class="star filled">★</span>
                {%- else -%}
                  <span class="star empty">☆</span>
                {%- endif -%}
              {%- endfor -%}
            </div>
            <div class="review-date">{{ block.settings.date | default: 'now' | date: '%Y-%m-%d' }}</div>
          </div>
          
          <div class="review-author">
            <strong>{{ block.settings.author | default: 'Cliente Verificado' }}</strong>
            {%- if block.settings.location -%}
              <span class="location">{{ block.settings.location }}</span>
            {%- endif -%}
            {%- if block.settings.verified -%}
              <span class="verified-badge">✓</span>
            {%- endif -%}
          </div>
          
          <div class="review-text">
            {{ block.settings.review_text }}
          </div>
          
          {%- if block.settings.image -%}
            <div class="review-image">
              <img src="{{ block.settings.image | image_url: width: 200 }}" 
                   alt="Review image" 
                   loading="lazy">
            </div>
          {%- endif -%}
        </div>
      {%- endfor -%}
    {%- endif -%}
  </div>
</div>

<style>
/* PROFESSIONAL MOBILE-FIRST STYLES */
.reviews-section-enhanced {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 16px;
}

.reviews-header-professional {
  background: #ffffff;
  padding: 24px 0;
  margin-bottom: 32px;
}

.reviews-header-content {
  display: flex;
  flex-direction: column;
  gap: 24px;
}

/* Title Section */
.reviews-title-group {
  text-align: center;
}

.reviews-title {
  font-size: 1.75rem;
  font-weight: 700;
  color: #1a202c;
  margin: 0 0 8px 0;
  line-height: 1.2;
  letter-spacing: -0.025em;
}

.reviews-subtitle {
  font-size: 0.95rem;
  color: #64748b;
  margin: 0;
  font-weight: 400;
  line-height: 1.4;
}

/* Rating Display - Mobile Optimized */
.reviews-rating-display {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  padding: 20px;
  background: #f8fafc;
  border-radius: 12px;
  border: 1px solid #e2e8f0;
}

.rating-score-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
}

.rating-score {
  font-size: 2.25rem;
  font-weight: 800;
  color: #1e293b;
  line-height: 1;
  letter-spacing: -0.025em;
}

.rating-info {
  text-align: center;
}

.stars-container {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 2px;
  margin-bottom: 8px;
}

.star {
  font-size: 1.25rem;
  line-height: 1;
}

.star-full {
  color: #f59e0b;
}

.star-half {
  color: #f59e0b;
  opacity: 0.5;
}

.star-empty {
  color: #d1d5db;
}

.rating-description {
  font-size: 0.875rem;
  color: #64748b;
  margin: 0;
  font-weight: 500;
}

/* Trust Badge */
.trust-badge {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 6px 12px;
  background: #dcfce7;
  color: #166534;
  border-radius: 20px;
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.check-icon {
  fill: currentColor;
  width: 14px;
  height: 14px;
}

/* Separator */
.reviews-separator {
  width: 60px;
  height: 2px;
  background: linear-gradient(90deg, transparent 0%, #cbd5e1 50%, transparent 100%);
  margin: 16px auto 0;
}

/* Reviews Content Styles */
.reviews-content {
  display: grid;
  gap: 24px;
}

.review-item {
  background: #ffffff;
  border-radius: 12px;
  padding: 24px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
  border: 1px solid #e2e8f0;
}

.review-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.review-rating .star.filled {
  color: #f59e0b;
}

.review-rating .star.empty {
  color: #d1d5db;
}

.review-date {
  font-size: 0.875rem;
  color: #64748b;
}

.review-author {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 12px;
}

.location {
  font-size: 0.875rem;
  color: #64748b;
}

.verified-badge {
  background: #dcfce7;
  color: #166534;
  padding: 2px 6px;
  border-radius: 12px;
  font-size: 0.75rem;
  font-weight: 600;
}

.review-text {
  color: #374151;
  line-height: 1.6;
  margin-bottom: 16px;
}

.review-image img {
  border-radius: 8px;
  max-width: 200px;
  height: auto;
}

/* TABLET STYLES */
@media (min-width: 640px) {
  .reviews-header-content {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    gap: 32px;
  }
  
  .reviews-title-group {
    text-align: left;
    flex: 1;
  }
  
  .reviews-title {
    font-size: 2rem;
  }
  
  .reviews-subtitle {
    font-size: 1rem;
  }
  
  .reviews-rating-display {
    flex: 0 0 auto;
    min-width: 280px;
  }
  
  .rating-score-container {
    flex-direction: row;
    gap: 16px;
  }
  
  .rating-info {
    text-align: center;
  }
  
  .stars-container {
    justify-content: center;
  }
  
  .reviews-content {
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  }
}

/* DESKTOP STYLES */
@media (min-width: 1024px) {
  .reviews-section-enhanced {
    padding: 0 24px;
  }
  
  .reviews-header-professional {
    padding: 32px 0;
  }
  
  .reviews-title {
    font-size: 2.25rem;
  }
  
  .reviews-rating-display {
    padding: 24px;
    min-width: 320px;
  }
  
  .rating-score {
    font-size: 2.5rem;
  }
}

/* HOVER EFFECTS */
@media (hover: hover) {
  .reviews-rating-display {
    transition: all 0.2s ease;
  }
  
  .reviews-rating-display:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
  }
  
  .review-item {
    transition: all 0.2s ease;
  }
  
  .review-item:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
  }
}
</style>

{% schema %}
{
  "name": "Reviews Visual Enhancement",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "Reseñas de Clientes"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Opiniones reales de nuestros compradores"
    },
    {
      "type": "number",
      "id": "rating",
      "label": "Overall Rating",
      "default": 4.88,
      "info": "Average rating (0-5)"
    },
    {
      "type": "number",
      "id": "review_count",
      "label": "Review Count",
      "default": 127
    }
  ],
  "blocks": [
    {
      "type": "review",
      "name": "Customer Review",
      "settings": [
        {
          "type": "text",
          "id": "author",
          "label": "Author Name",
          "default": "Cliente Verificado"
        },
        {
          "type": "text",
          "id": "location",
          "label": "Location"
        },
        {
          "type": "number",
          "id": "rating",
          "label": "Rating",
          "default": 5,
          "info": "Rating (1-5 stars)"
        },
        {
          "type": "textarea",
          "id": "review_text",
          "label": "Review Text"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Review Image (Optional)"
        },
        {
          "type": "text",
          "id": "date",
          "label": "Review Date",
          "info": "Format: YYYY-MM-DD"
        },
        {
          "type": "checkbox",
          "id": "verified",
          "label": "Verified Purchase",
          "default": true
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Reviews Visual Enhancement",
      "blocks": [
        {
          "type": "review",
          "settings": {
            "author": "Eduardo",
            "location": "🇩🇴",
            "rating": 5,
            "review_text": "Más de 5 años usando la marca excelente",
            "date": "2025-07-24",
            "verified": true
          }
        }
      ]
    }
  ]
}
{% endschema %}
